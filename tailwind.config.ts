// Tailwind v4: all theme tokens (colors, fonts) are configured in app/globals.css
// via the @theme {} block. This file is intentionally empty.
import type { Config } from 'tailwindcss'
const config: Config = {}
export default config
